# --- Core web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Retrieval / embeddings / LLM ---
numpy==2.2.1
anthropic==0.42.0
openai==1.59.6

# --- Document parsing ---
pypdf==5.1.0

# --- Dev / test (kept here so `pip install -r` gives a working CI) ---
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
